<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#451F11" />
  <meta name="robots" content="index,follow" />
  <title>{% if title and page.url != '/' %}{{ title }}{% else %}Emily McCracken{% endif %} - {{ site.title }}</title>
  <meta name="description" content="{% if description %}{{ description | markdownify_inline | strip_html }}{% else %}{{ site.description }}{% endif %}" />
  <meta property="og:title" content="{% if title and page.url != '/' %}{{ title }}{% else %}Emily McCracken{% endif %} - {{ site.title }}">
  <meta property="og:description" content="{% if description %}{{ description | markdownify_inline | strip_html }}{% else %}{{ site.description }}{% endif %}">
  <meta property="og:url" content="{{ site.url }}{{ page.url | permalink }}">
  <meta property="og:image" content="{{ site.url }}{%if thumbnail %}{{ thumbnail }}{% else %}/assets/img/og-image.jpg{% endif %}">
  <meta name="twitter:card" content="summary">
  <link rel="icon apple-touch-icon" href="/assets/img/apple-touch-icon.png" sizes="180x180" type="image/png" />
  <link rel="icon" href="/assets/img/favicon-96x96.png" sizes="96x96" type="image/png" />
  <link rel="icon" href="/assets/img/favicon-32x32.png" sizes="32x32" type="image/png" />
  <link rel="icon" href="/assets/img/favicon-16x16.png" sizes="16x16" type="image/png" />
  <link rel="manifest" href="/manifest.json" />
  <link rel="alternate" type="application/rss+xml" title="Feed for journal" href="/journal/feed.xml" />
  <link rel="alternate" type="application/rss+xml" title="Feed for sculptures" href="/sculptures/feed.xml" />
  <link href="https://fonts.googleapis.com/css?family=Bitter:400,700&amp;display=swap" rel="stylesheet">
  {% set css %}
    {% include "assets/screen.css" %}
    {% if primaryColor %}
      :root { --color-primary: {{ primaryColor }}; }
    {% endif %}
  {% endset %}
  <style>{{ css | cssmin | safe }}</style>
</head>
<body>
  <a href="#main-content" class="c-skip">Skip to main content</a>
  <div class="o-container">
    {% include "components/header.njk" %}
    <main id="main-content">
      {% block header %}{% endblock %}
      {% block content %}{% endblock %}
    </main>
  </div>
  {% include "components/footer.njk" %}
</body>
</html>